---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [Cargo.toml, src/main.rs]
---

<objective>
Initialize Rust project with ratatui TUI framework dependencies.

Purpose: Create the project foundation that all subsequent development builds on.
Output: Working Cargo project that compiles with ratatui and crossterm dependencies.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Cargo project</name>
  <files>Cargo.toml, src/main.rs</files>
  <action>Run `cargo init` in project root to create Cargo.toml and src/main.rs. Set package name to "pretty-table-explorer" in Cargo.toml. Set edition to "2021".</action>
  <verify>Cargo.toml exists with [package] section, src/main.rs exists</verify>
  <done>cargo init completed, package named "pretty-table-explorer", edition 2021</done>
</task>

<task type="auto">
  <name>Task 2: Add TUI dependencies</name>
  <files>Cargo.toml</files>
  <action>Add dependencies to Cargo.toml:
- ratatui = "0.29" (TUI framework)
- crossterm = "0.28" (terminal backend, must be compatible with ratatui version)

Use `cargo add ratatui crossterm` to add with correct versions. Verify versions are compatible by checking ratatui docs if needed.</action>
  <verify>cargo check succeeds without dependency resolution errors</verify>
  <done>Dependencies added, cargo check passes, no version conflicts</done>
</task>

<task type="auto">
  <name>Task 3: Verify project builds</name>
  <files>src/main.rs</files>
  <action>Run `cargo build` to compile the project. The default main.rs (hello world) should compile. If build fails, fix any issues before proceeding.</action>
  <verify>cargo build succeeds with exit code 0</verify>
  <done>Project builds successfully, target/debug/pretty-table-explorer binary exists</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check` passes
- [ ] `cargo build` succeeds
- [ ] Cargo.toml contains ratatui and crossterm dependencies
- [ ] Binary exists at target/debug/pretty-table-explorer
</verification>

<success_criteria>

- Cargo project initialized with correct name
- ratatui and crossterm dependencies added
- Project compiles without errors
- Ready for Plan 01-02 (terminal UI scaffold)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
